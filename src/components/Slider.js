import React from 'react';

const Slider = (props) => {
    return(
        <div className="section section-slider">
            <img src={props.sliderImg} alt="Slider" />
        </div>
    )
}


export default Slider;